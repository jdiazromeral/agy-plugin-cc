"""companion — the Python program that wraps agy.

Structured as a package with a subcommand dispatch (see cli.py) so a new
subcommand is one sibling module plus one line in the dispatch table, with
no restructuring.
"""
