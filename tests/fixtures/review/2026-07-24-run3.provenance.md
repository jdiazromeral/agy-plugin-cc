# Fixture provenance: 2026-07-24-run3.stdout.txt

Captured by `tools/live_review_capture.py` (mission M2, ledger row 5) against
a throwaway scratch repo, never committed anywhere, never `lab/agy-plugin-cc`.

- **Date**: 2026-07-24
- **agy version**: 1.1.6
- **Bound agent**: `agy-review` — proof (quoted from `--log-file`):
  `server.go:934] Created conversation 52e2439e-241b-4828-b394-dcf07abe3377`,
  and the log contains **no** `Agent "agy-review" not found, falling back to
  default` line (the **silent fallback** pattern), which is how a **bind** is
  distinguished from a fallback per this mission's free bind probe.
- **Conversation UUID**: `52e2439e-241b-4828-b394-dcf07abe3377`
- **Model resolved**: `Gemini 3.1 Pro (High)` (no `--model` flag passed; this
  is agy's own default for the account used)
- **Command**:
  ```
  agy -p "<prompt below>" --agent agy-review --sandbox --new-project --log-file <path>
  ```
  run with `cwd` set to the scratch repo root, `stdin` closed
  (`subprocess.DEVNULL`).
- **Agent placement**: `.agents/agents/agy-review/agent.md` inside the
  scratch repo (workspace-scoped, per Finding B — requires `--new-project`
  to bind; see `docs/review-schema-verdict.md`).
- **Prompt** (the diff is embedded directly in the prompt text; `--sandbox`
  soft-denies tool calls in headless print mode, so the agent is never asked
  to discover the diff itself):
  ```
  Review the following working tree diff for bugs. Output only the JSON
  described in your system instructions, nothing else.

  ```diff
  diff --git a/calc.py b/calc.py
  index b2db1f1..79278a0 100644
  --- a/calc.py
  +++ b/calc.py
  @@ -1,3 +1,3 @@
   def add(a, b):
       """Add two numbers."""
  -    return a + b
  +    return a - b  # bug: should be a + b
  ```
  ```
- **Result**: valid top-level JSON, all four required keys present
  (`findings`, `overall_correctness`, `overall_explanation`,
  `overall_confidence_score`), no markdown fences, no prose preamble/postamble
  — stdout is the JSON object and nothing else. `code_location.
  absolute_file_path` is a genuinely absolute path in this run.
- **Redaction (2026-07-28, pre-publication)**: the captured
  `code_location.absolute_file_path` was the scratch repo's real path under a
  local scratchpad directory, which embedded the capturing user's username and
  workspace layout. It was rewritten to `/tmp/captures/run3-repo/calc.py` —
  still absolute, preserving the evidentiary point of this fixture (run3
  returns an absolute path where run4 returns a bare relative `calc.py`; see
  `docs/review-schema-verdict.md`, "Deviation actually observed"). Only the
  directory prefix changed; the basename, every other byte of the JSON, and
  the file's structure are as captured.
