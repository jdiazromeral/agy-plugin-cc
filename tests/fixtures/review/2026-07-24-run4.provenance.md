# Fixture provenance: 2026-07-24-run4.stdout.txt

Captured by `tools/live_review_capture.py` (mission M2, ledger row 6) against
a throwaway scratch repo, never committed anywhere, never `lab/agy-plugin-cc`.
Same bootstrap, prompt template, and agent placement as
`2026-07-24-run3.provenance.md` (a fresh scratch repo per run, same seeded
bug) — repeated back-to-back to get a second data point on schema
consistency.

- **Date**: 2026-07-24
- **agy version**: 1.1.6
- **Bound agent**: `agy-review` — proof (quoted from `--log-file`):
  `server.go:934] Created conversation b49d685e-3756-4552-b71d-113dfacc9e2f`,
  and the log contains **no** `Agent "agy-review" not found, falling back to
  default` line.
- **Conversation UUID**: `b49d685e-3756-4552-b71d-113dfacc9e2f`
- **Model resolved**: `Gemini 3.1 Pro (High)`
- **Command**:
  ```
  agy -p "<prompt below>" --agent agy-review --sandbox --new-project --log-file <path>
  ```
  run with `cwd` set to the scratch repo root, `stdin` closed
  (`subprocess.DEVNULL`).
- **Agent placement**: `.agents/agents/agy-review/agent.md` inside the
  scratch repo (workspace-scoped, requires `--new-project` to bind).
- **Prompt**: identical template and diff as run3 (see its provenance file
  for the full text) — same seeded bug (`calc.py`, `a - b` instead of
  `a + b`).
- **Result**: valid top-level JSON, all four required keys present, no
  markdown fences, no prose preamble/postamble. **Deviation observed**:
  `code_location.absolute_file_path` is `"calc.py"` — a **relative** path,
  not absolute, despite the schema's field name and the vendored prompt's
  explicit requirement ("The `code_location` field is required and must
  include `absolute_file_path`"). This is the one concrete schema deviation
  this mission's sample caught; see `docs/review-schema-verdict.md` for the
  full analysis and what M4's **tolerant parse** should do about it.
